import React from "react";

const Header = () => {
  return (
    <div>
      <header class="header">
        <span class="logo">Мини резюме</span>
      </header>
    </div>
  );
};

export default Header;
